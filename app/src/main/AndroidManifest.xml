<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mustafa.a_market">


    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="27" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" />
    <uses-feature android:name="android.hardware.camera2.autofocus" />
    //Kamerayı Açmak İçin Gerekli İzinler(Ramazan Kara)
    <uses-permission android:name="android.permission.INTERNET" />
    //Internet Acma izni alındı

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Arama_Activity" />
        <activity android:name=".barcodeShow" />
        // Yeni Activity için İzin(Ramazan Kara)
        <activity android:name=".Yonlendirme_Activity" />
        <activity android:name=".webViewActivity" />
        <activity android:name=".callActivity"></activity>
    </application>

</manifest>